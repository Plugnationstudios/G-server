# Server
PORT=3000
NODE_ENV=development

# Domain base for subdomains (replace with your duckdns domain or real domain)
BASE_DOMAIN=gserverjr.duckdns.org

# Secrets (generate strong random strings)
UPLOAD_SECRET=sdm,sds.dmsldl
ADMIN_TOKEN=kdklslklsdklsd
JWT_SECRET=klk
# Limits / rate limit
MAX_UPLOAD_BYTES=52428800
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120

# Storage: switch to true to use AWS S3 (durable). Otherwise local filesystem is used.
USE_S3=false
AWS_REGION=us-east-1
S3_BUCKET=

# Logging level
LOG_LEVEL=info
